<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Second-Hand Marketplace</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<header class="bg-blue-600 text-white p-4 flex justify-between">
    <h1 class="text-2xl">Second-Hand Marketplace</h1>
    <nav>
        <% if (typeof session.userId !== 'undefined') { %>
            <a href="/auth/logout" class="text-white hover:underline">Logout</a>
        <% } else { %>
            <a href="/auth/login" class="text-white hover:underline">Login</a>
            <a href="/auth/register" class="ml-4 text-white hover:underline">Register</a>
        <% } %>
    </nav>
</header>

<body class="bg-gray-100">
    <header class="bg-blue-600 text-white p-4">
        <h1 class="text-center text-2xl">Second-Hand Marketplace</h1>
    </header>
    <main class="p-4">
        <%= body %>
    </main>
    <script src="/js/app.js"></script>
</body>
</html>
